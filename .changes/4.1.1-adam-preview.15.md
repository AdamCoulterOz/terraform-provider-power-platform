## 4.1.1-adam-preview.15 - 2026-04-22

- Preview build for `codex/preview-integration`.
- Renames `powerplatform_environment_application_user` to `powerplatform_application_user`.
- Introduces `powerplatform_role_assignment` with one Dataverse security role assignment per principal.
- Updates the application user contract so `id` is the Dataverse principal identifier used by downstream role assignments.
